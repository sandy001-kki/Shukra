{{- if and .Values.bridge.enabled .Values.bridge.apiKey }}
apiVersion: v1
kind: Secret
metadata:
  name: shukra-bridge-aionos
  namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
  api-key: {{ .Values.bridge.apiKey | quote }}
{{- end }}
